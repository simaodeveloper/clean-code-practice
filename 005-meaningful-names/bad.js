// Meaningful names

const getUserData = () => { };
const getUserInfo = () => { };
const getUserPosts = () => { };

// Make booleans that read well in if-then statements

// Change these names
let Car = {
  sedan: true,
  sold: false,
  green: true,
  airbag: false
};


// Use nouns for classNames

// Dont use
class MakeCar { }
