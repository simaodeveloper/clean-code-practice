// Stop writing comments

// Avoid Large functions

const addMultiplySubtract = (a, b, c) => {
  // adding
  const adding = a + b + c;
  // multiplying
  const multiplying = a + b + c;
  // subtracting
  const subtracting = a + b + c;

  return `${adding} ${multiplying} ${subtracting}`;
}
